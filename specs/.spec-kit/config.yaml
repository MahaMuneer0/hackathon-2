name: hackathon-todo-phase2
version: "1.0"

structure:
  specs_dir: specs
  features_dir: specs/features
  api_dir: specs/api
  database_dir: specs/database
  ui_dir: specs/ui

phases:
  - name: phase1-console
    features: [task-crud]
    status: completed
  - name: phase2-web
    features: [task-crud, authentication, web-ui]
    status: in-progress

agents:
  - name: architect
    role: "Creates specifications and system design"
  - name: database
    role: "Manages database schema and models"
  - name: backend
    role: "Implements FastAPI endpoints"
  - name: frontend
    role: "Builds Next.js UI components"